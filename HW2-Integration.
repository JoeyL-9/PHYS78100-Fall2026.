import numpy as np
import matplotlib.pyplot as plt

def E(x):
    t = np.linspace(0, x, 1001)
    return np.trapz(np.exp(-t**2), t)

x = np.arange(0, 3.1, 0.1)
y = [E(i) for i in x]

print("E(3) =", E(4))

plt.plot(x, y)
plt.xlabel("x")
plt.ylabel("E(x)")
plt.grid()
plt.show()
